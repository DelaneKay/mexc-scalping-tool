{
  "name": "@mexc-scalping/api",
  "version": "1.0.0",
  "private": true,
  "description": "Netlify Functions API for MEXC scalping tool",
  "scripts": {
    "build": "tsc",
    "dev": "netlify dev",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@mexc-scalping/shared": "*",
    "@netlify/functions": "^2.4.0",
    "ccxt": "^4.1.0",
    "node-fetch": "^3.3.0",
    "protobufjs": "^7.2.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "netlify-cli": "^17.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}